all molpopcep calculations are done in molpop-cep-master
the file molpop.inp defines which models should be calculated. the executable molpop reades this file and calculates all models
the models themselves are specified files that are located in the folder Gianni_model. the results for each model are also placed in that folder (.out files)